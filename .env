# Base URL of the Django REST API
VITE_API_URL="http://localhost:8000/api"

# Which brand this storefront serves. The API holds each brand in its own
# database and requires this on every request.
VITE_TENANT_SLUG="cutcult"
